<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/category_iv_cover"
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:src="@color/purple_200"
        android:transitionName="image_cover"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearance="@style/RoundedStyle" />

    <TextView
        android:id="@+id/category_tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="#动画"
        android:textColor="@color/white"
        android:textSize="24sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/category_iv_cover"
        app:layout_constraintStart_toStartOf="@+id/category_iv_cover"
        app:layout_constraintTop_toTopOf="@+id/category_iv_cover" />

</androidx.constraintlayout.widget.ConstraintLayout>